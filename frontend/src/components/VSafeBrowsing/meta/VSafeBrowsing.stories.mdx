import { Canvas, Description, Meta, Story } from "@storybook/addon-docs"

import VSafeBrowsing from "~/components/VSafeBrowsing/VSafeBrowsing.vue"
import { WithSuspense } from "~~/.storybook/decorators/with-suspense"

<Meta title="Components/VSafeBrowsing" component={VSafeBrowsing} decorators={WithSuspense}/>

export const Template = (args) => ({
  template: `<VSafeBrowsing v-bind="args" />`,
  components: { VSafeBrowsing },
  setup() {
    return { args }
  },
})

# VSafeBrowsing

<Description of={VSafeBrowsing} />

<Canvas>
  <Story
    name="Default"
    parameters={{
      viewport: { defaultViewport: "xs" },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
