import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from "@storybook/addon-docs"

import VPill from "~/components/VPill.vue"

<Meta title="Components/VPill" components={VPill} />

export const Default = (args) => ({
  template: `<VPill>Beta</VPill>`,
  components: { VPill },
  setup() {
    return { args }
  },
})

<Description of={VPill} />

<ArgsTable of={VPill} />

## Default VPill

<Canvas>
  <Story name="Default">{Default.bind({})}</Story>
</Canvas>
