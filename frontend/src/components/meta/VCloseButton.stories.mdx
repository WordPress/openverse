import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from "@storybook/addon-docs"
import VCloseButton from "~/components/VCloseButton.vue"

<Meta title="Components/VCloseButton" components={VCloseButton} />

export const buttonVariants = [
  "filled-white",
  "filled-white-light",
  "filled-transparent",
  "filled-dark",
  "black",
]

export const Template = (args) => ({
  template: `
    <div class="flex"><div id="wrapper"
        class="px-4 h-16 flex flex-grow-0 items-center justify-center bg-dark-charcoal-06">
    <VCloseButton v-bind="args" v-on="args" label="Close" />
    </div></div>
  `,
  components: { VCloseButton },
  setup() {
    return { args }
  },
})

# VCloseButton

<Description of={VCloseButton} />

<Canvas>
  <Story
    name="VCloseButton"
    parameters={{
      viewport: { defaultViewport: "sm" },
    }}
    args={{
      variant: "filled-white-light",
    }}
    argTypes={{
      variant: {
        options: buttonVariants,
        control: { type: "select" },
      },
      size: {
        options: ["small", "medium", "large"],
        control: { type: "select" },
      },
      iconSize: {
        options: ["small", "medium"],
        control: { type: "select" },
      },
      close: {
        action: "close",
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={VCloseButton} />

## VCloseButton variants

export const VariantsTemplate = (args) => ({
  template: `
    <div class="flex p-4 gap-4 items-center justify-center bg-dark-charcoal-06" id="wrapper">
    <VCloseButton
        v-for="variant in buttonVariants"
        :key="variant" v-bind="{...args, variant }"
        v-on="args" label="Close" />
    </div>
  `,
  components: { VCloseButton },
  setup() {
    return { args }
  },
  data: () => ({
    buttonVariants,
  }),
})

<Canvas>
  <Story
    name="VCloseButton varuabts"
    parameters={{
      viewport: { defaultViewport: "sm" },
    }}
    argTypes={{
      size: {
        options: ["small", "medium", "large"],
        control: { type: "select" },
      },
      iconSize: {
        options: ["small", "medium"],
        control: { type: "select" },
      },
      close: {
        action: "close",
      },
    }}
  >
    {VariantsTemplate.bind({})}
  </Story>
</Canvas>
