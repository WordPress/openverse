import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from "@storybook/addon-docs"

import VOldIconButton from "~/components/VIconButton/VOldIconButton.vue"

<Meta
  title="Components/VOldIconButton"
  component={VOldIconButton}
  argTypes={{
    size: {
      defaultValue: "medium",
      options: ["tiny", "small", "medium", "large", "extra-large"],
      control: "select",
    },
  }}
/>

export const Template = (args) => ({
  template: `
    <VOldIconButton class="text-white bg-dark-charcoal border-dark-charcoal" v-bind="args" label="v-icon-button" />
  `,
  components: { VOldIconButton },
  setup() {
    return { args }
  },
})

export const sizesTemplate = (args) => ({
  template: `
    <div class="flex gap-x-2">
      <div v-for="size in ['tiny', 'small', 'medium', 'large', 'extra-large']" :key="size" class="flex flex-col items-center p-2">
        <p>{{ size }}</p>
        <VOldIconButton
           class="text-white bg-dark-charcoal border-dark-charcoal"
           label="v-icon-button"
           :size="size" v-bind="args" />
      </div>
    </div>
  `,
  components: { VOldIconButton },
  setup() {
    return { args }
  },
})

# Icon Button

<Description of={VOldIconButton} />

<ArgsTable of={VOldIconButton} />

<Canvas>
  <Story
    name="Default"
    args={{
      iconProps: {
        name: "replay",
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Sizes

<Canvas>
  <Story
    name="Sizes"
    args={{
      iconProps: {
        name: "replay",
      },
    }}
  >
    {sizesTemplate.bind({})}
  </Story>
</Canvas>
