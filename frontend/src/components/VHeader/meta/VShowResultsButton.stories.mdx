import {
  Meta,
  Description,
  ArgsTable,
  Canvas,
  Story,
} from "@storybook/addon-docs"

import VShowResultsButton from "~/components/VHeader/VHeaderMobile/VShowResultsButton.vue"
import { WithSuspense } from "~~/.storybook/decorators/with-suspense"

<Meta
  title="Components/VHeader/VHeaderMobile/VShowResultsButton"
  component={VShowResultsButton}
  decorators={WithSuspense}
/>

export const Template = (args) => ({
  template: `<VShowResultsButton v-bind="args"/>`,
  setup() {
    return { args }
  },
})

# VShowResultsButton

<Description of={VShowResultsButton} />

<ArgsTable of={VShowResultsButton} />

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

<Canvas>
  <Story name="Fetching" args={{ isFetching: true }}>
    {Template.bind({})}
  </Story>
</Canvas>
