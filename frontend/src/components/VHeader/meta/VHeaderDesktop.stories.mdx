import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from "@storybook/addon-docs"
import { provide, ref } from "vue"
import { IsSidebarVisibleKey } from "~/types/provides"
import VHeaderDesktop from "~/components/VHeader/VHeaderDesktop.vue"

<Meta title="Components/VHeader/VHeaderDesktop" component={VHeaderDesktop} />

export const Template = () => ({
  template: `<VHeaderDesktop />`,
  components: { VHeaderDesktop },
  setup() {
    provide(IsSidebarVisibleKey, ref(false))
    return {}
  },
})

# VHeaderDesktop

<Description of={VHeaderDesktop} />

<ArgsTable of={VHeaderDesktop} />

The header component used for screens on breakpoints wider than `lg`. Fixed on
scroll, search bar, content switcher, pages menu and filters button.

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>
