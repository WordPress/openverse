import { Description, Canvas, Meta, Story } from "@storybook/addon-docs"

import VTag from "~/components/VTag/VTag.vue"

<Meta
  title="Components/VTag"
  component={VTag}
/>

export const Template = (args) => ({
  template: `<VTag v-bind="args"/>`,
  components: { VTag },
  setup() {
    return { args }
  },
})
<Description of={VTag} />

<Canvas>
  <Story name="default" args={{ href: "#", title: "cat" }}>
    {Template.bind({})}
  </Story>
</Canvas>
