import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from "@storybook/addon-docs"

import VHomeGallery from "~/components/VHomeGallery/VHomeGallery.vue"
import { WithSuspense } from "~~/.storybook/decorators/with-suspense"

<Meta
  title="Components/VHomeGallery"
  component={VHomeGallery}
  decorators={WithSuspense}
  argTypes={{
    set: {
      defaultValue: "random",
      options: ["random", "olympics", "pottery", "universe"],
      control: "select",
    },
  }}
/>

export const Template = (args) => ({
  template: `<VHomeGallery v-bind="args" />`,
  components: { VHomeGallery },
  setup() {
    return { args }
  },
})

# VHomeGallery

<Description of={VHomeGallery} />

<ArgsTable of={VHomeGallery} />

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>
