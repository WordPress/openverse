import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs'

import { ref } from '@nuxtjs/composition-api'

import VSearchTypePopoverOld from '~/components/VContentSwitcherOld/VSearchTypePopoverOld.vue'

<Meta
  title="Components/VSearchTypePopoverOld"
  components={VSearchTypePopoverOld}
  argTypes={{
    select: {
      action: 'select',
    },
  }}
/>

export const Template = (args) => ({
  template: `<VSearchTypePopoverOld active-item="image" v-bind="args" v-on="args"/>`,
  components: { VSearchTypePopoverOld },
  setup() {
    return { args }
  },
})

# VSearchTypePopoverOld

<Description of={VSearchTypePopoverOld} />

<ArgsTable of={VSearchTypePopoverOld} />

<Canvas>
  <Story height="480px" name="Default">
    {Template.bind({})}
  </Story>
</Canvas>

## v-model

The recommended way to use it is with `v-model` mapping to a `String`.

export const vModelTemplate = () => ({
  template: `
    <div>
      {{ item }}
      <VSearchTypePopoverOld v-model="item"/>
    </div>
  `,
  components: { VSearchTypePopoverOld },
  setup() {
    const item = ref('all')
    return { item }
  },
})

<Canvas>
  <Story height="480px" name="v-model">
    {vModelTemplate.bind({})}
  </Story>
</Canvas>
