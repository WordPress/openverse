import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs'

import VNavLink from '~/components/VNavLink/VNavLink.vue'

export const Template = (args) => ({
  template: `<VNavLink link="/about" v-bind="args" v-on="args">About</VNavLink>`,
  components: { VNavLink },
  setup() {
    return { args }
  },
})

<Meta
  title="Components/VNavLink"
  component={VNavLink}
  argTypes={{
    isActive: { control: { type: 'boolean' } },
    mode: { control: { type: 'select', options: ['dark', 'light'] } },
    variant: { control: { type: 'select', options: ['inline', 'column'] } },
    link: { control: { type: 'text' } },
    click: { action: 'click' },
  }}
/>

# VNavLink

<Description of={VNavLink} />

<ArgsTable of={VNavLink} />

<Canvas>
  <Story name="default">{Template.bind({})}</Story>
</Canvas>
