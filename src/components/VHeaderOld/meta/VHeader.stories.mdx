import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs'
import VHeaderOld from '~/components/VHeaderOld/VHeaderOld.vue'

<Meta
  title="Components/VHeader/VHeader"
  component={VHeaderOld}
  argTypes={{
    input: {
      action: 'input',
    },
    submit: {
      action: 'submit',
    },
  }}
/>

export const Template = (args) => ({
  template: `
    <VHeaderOld v-bind="args" v-on="args" />`,
  components: { VHeaderOld },
  setup() {
    return { args }
  },
})

# VHeader

<Description of={VHeaderOld} />

<ArgsTable of={VHeaderOld} />

The header component. Fixed on scroll, search bar, content switcher, pages menu and filters button.

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>
