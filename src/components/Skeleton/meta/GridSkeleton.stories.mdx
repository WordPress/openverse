import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs'

import GridSkeleton from '~/components/Skeleton/GridSkeleton.vue'

<Meta title="Components/Skeleton" component={GridSkeleton} />

export const Template = (args, { argTypes }) => ({
  template: `<GridSkeleton v-bind="$props" />`,
  components: { GridSkeleton },
  props: Object.keys(argTypes),
})

# Skeleton

<Description of={GridSkeleton} />

<ArgsTable of={GridSkeleton} />

## Grid skeleton for "All" tab

<Canvas>
  <Story name="All tab" args={{ isForTab: 'all' }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Grid skeleton for "Image" tab

<Canvas>
  <Story name="Image tab" args={{ isForTab: 'image' }}>
    {Template.bind({})}
  </Story>
</Canvas>

## List skeleton for "Audio" tab

<Canvas>
  <Story name="Audio tab" args={{ isForTab: 'audio' }}>
    {Template.bind({})}
  </Story>
</Canvas>
