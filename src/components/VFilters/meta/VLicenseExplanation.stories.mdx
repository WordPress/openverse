import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from "@storybook/addon-docs"

import VLicenseExplanation from "~/components/VFilters/VLicenseExplanation.vue"

import { ALL_LICENSES } from "~/constants/license"

<Meta
  title="Components/VLicenseExplanation"
  components={VLicenseExplanation}
  argTypes={{
    license: {
      defaultValue: ALL_LICENSES[0],
      options: ALL_LICENSES,
      control: "select",
    },
  }}
/>

export const Template = (args) => ({
  template: `<VLicenseExplanation v-bind="args" v-on="args"/>`,
  components: { VLicenseExplanation },
  setup() {
    return { args }
  },
})

# VLicenseExplanation

<Description of={VLicenseExplanation} />

<ArgsTable of={VLicenseExplanation} />

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>
