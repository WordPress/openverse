import store from '@/store/filter-store';
import { TOGGLE_FILTER } from '@/store/action-types';
import { SET_FILTER, SET_PROVIDERS_FILTERS, CLEAR_FILTERS } from '@/store/mutation-types';

describe('Filter Store', () => {
  describe('state', () => {
  });
});
